---
import { getImage } from "astro:assets";
import { OG, SEO, SITE } from "@data/constants";
import faviconSvgSrc from "@images/icon.svg";
import faviconSrc from "@images/icon.png";
import { locales } from "@utils/ui";

// Default properties for the Meta component. These values are used if props are not provided.
// 'meta' sets a default description meta tag to describe the page content.
// 'structuredData' defines default structured data in JSON-LD format to enhance search engine understanding of the page (for SEO purposes).
const defaultProps = {
  meta: SITE.description,
  structuredData: SEO.structuredData,
};

interface Props {
  meta?: string;
  structuredData?: object;
  social?: {
    title?: string;
    description?: string;
    image?: string;
    imageType?: string;
    url?: string;
    type?: string;
  };
}

// Extract props with default values assigned from defaultProps. Values can be overridden when the component is used.
// For example:
// <MainLayout title="Custom Title" meta="Custom description." />
const {
  meta = defaultProps.meta,
  structuredData = defaultProps.structuredData,
  social,
} = Astro.props as Props;

// Define the metadata for your website and individual pages
const URL = `${Astro.site}`; // Set the website URL in astro.config.mjs
const author = SITE.author;
const canonical = Astro.url.href;
const ogTitle = social?.title ?? OG.title;
const ogDescription = social
  ? (social.description ?? meta ?? OG.description)
  : OG.description;
const ogType = social?.type ?? "website";
const ogUrl = social !== undefined ? (social.url ?? canonical) : URL;

let socialImage: string;
let socialImageWidth: number;
let socialImageHeight: number;
let socialImageType: string;

if (social?.image) {
  socialImage = new globalThis.URL(
    social.image,
    Astro.site ?? Astro.url.origin,
  ).href;
  socialImageWidth = 1200;
  socialImageHeight = 630;
  socialImageType = social.imageType ?? "image/png";
} else {
  const socialImageRes = await getImage({
    src: OG.image,
    width: 1200,
    height: 600,
  });
  socialImage = Astro.url.origin + socialImageRes.src; // Get the full URL of the image (https://stackoverflow.com/a/9858694)
  socialImageWidth = 1200;
  socialImageHeight = 600;
  socialImageType = "image/png";
}

const localePrefixes: Record<string, string> = {
  en: "",
  es: "es",
  pt: "pt",
};

const ogLocaleMap: Record<string, string> = {
  en: "en_US",
  es: "es_ES",
  pt: "pt_BR",
};

const routeLocalePrefixes = ["es", "pt"];

function stripLocalePrefix(path: string): string {
  const parts = path.split("/").filter(Boolean);
  if (parts.length > 0 && routeLocalePrefixes.includes(parts[0])) {
    return "/" + parts.slice(1).join("/");
  }
  return path;
}

function createHref(lang: string, path: string): string {
  const basePath = stripLocalePrefix(path);
  const prefix = localePrefixes[lang];
  if (!prefix) {
    return `${URL.slice(0, -1)}${basePath || "/"}`;
  }
  const localized =
    basePath === "/" || basePath === ""
      ? `/${prefix}`
      : `/${prefix}${basePath}`;
  return `${URL.slice(0, -1)}${localized}`;
}

const fullPath: string = Astro.url.pathname;

const alternateLanguageLinks: string = locales
  .map((lang) => {
    const href = createHref(lang, fullPath);
    return `<link rel="alternate" hreflang="${lang}" href="${href}" />`;
  })
  .join("\n");

const currentOgLocale =
  ogLocaleMap[Astro.currentLocale ?? "en"] ?? ogLocaleMap.en;

// Generate and optimize the favicon images
const faviconSvg = await getImage({
  src: faviconSvgSrc,
  format: "svg",
});

const appleTouchIcon = await getImage({
  src: faviconSrc,
  width: 180,
  height: 180,
  format: "png",
});
---

<!-- Inject structured data into the page if provided. This data is formatted as JSON-LD, a method recommended by Google for structured data pass:
     https://developers.google.com/search/docs/advanced/structured-data/intro-structured-data -->{
  structuredData && (
    <script
      type="application/ld+json"
      is:inline
      set:html={JSON.stringify(structuredData)}
    />
  )
}
<!-- Define the character set, description, author, and viewport settings -->
<meta charset="utf-8" />
<meta content={meta} name="description" />
<meta name="web_author" content={author} />
<meta
  name="viewport"
  content="width=device-width, initial-scale=1.0, maximum-scale=5.0, minimum-scale=1.0"
/>
<meta http-equiv="X-UA-Compatible" content="ie=edge" />
<link rel="canonical" href={canonical} />
<Fragment set:html={alternateLanguageLinks} />

<!-- Facebook Meta Tags -->
<meta property="og:locale" content={currentOgLocale} />
<meta property="og:url" content={ogUrl} />
<meta property="og:type" content={ogType} />
<meta property="og:title" content={ogTitle} />
<meta property="og:site_name" content={SITE.title} />
<meta property="og:description" content={ogDescription} />
<meta property="og:image" content={socialImage} />
<meta content={String(socialImageWidth)} property="og:image:width" />
<meta content={String(socialImageHeight)} property="og:image:height" />
<meta content={socialImageType} property="og:image:type" />

<!-- Twitter Meta Tags -->
<meta name="twitter:card" content="summary_large_image" />
<meta property="twitter:domain" content={URL} />
<meta property="twitter:url" content={ogUrl} />
<meta name="twitter:title" content={ogTitle} />
<meta name="twitter:description" content={ogDescription} />
<meta name="twitter:image" content={socialImage} />

<!-- Links to the webmanifest and sitemap -->
<link rel="manifest" href="/manifest.json" />
<!-- https://docs.astro.build/en/guides/integrations-guide/sitemap/ -->
<link rel="sitemap" href="/sitemap-index.xml" />

<!-- Links for favicons -->
<link href="/favicon.ico" rel="icon" sizes="any" type="image/x-icon" />
<link href={faviconSvg.src} rel="icon" type="image/svg+xml" sizes="any" />
<meta name="mobile-web-app-capable" content="yes" />
<link href={appleTouchIcon.src} rel="apple-touch-icon" />
<link href={appleTouchIcon.src} rel="shortcut icon" />
<!-- Set theme color -->
<meta name="theme-color" content="#facc15" />
